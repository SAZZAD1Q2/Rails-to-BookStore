<h1>Hi am very tierx</h1>

      <ul>
            <% @books.each do |book| %>
              <li> 
                 <a href="<%= book_path(book) %>">
        <%= book.name %>
      </a>
                  </li>
            <% end %>
      </ul>

      
<%= link_to "Add New Book", new_book_path %>
